mixin parseDateTime(time)
    div #{moment(time).format('DD/MM/YYYY, HH:mm:ss')}

mixin parseDate(time)
    div #{moment(time).format('DD/MM/YYYY')}

mixin parseTime(time)
    div #{moment(time).format('HH:mm:ss')}
